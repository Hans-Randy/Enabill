CREATE TABLE [dbo].[PrintOptions] (
    [PrintOptionID]   INT           NOT NULL,
    [PrintOptionName] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_PrintOptions] PRIMARY KEY CLUSTERED ([PrintOptionID] ASC)
);

