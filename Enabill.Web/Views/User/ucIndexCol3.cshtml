@if (CurrentUser.HasRole(Enabill.UserRoleType.SystemAdministrator))
{
    <div class="actionDiv" onclick="window.location = '/User/Create/';">
        <span class="imageWithText">
            <img alt="" height="18px" src="@Url.Content("~/Content/Img/add_plus_green.png")" />
        </span>
        Create New
    </div>
}