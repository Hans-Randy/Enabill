@model Enabill.Web.Models.LeaveIndexModel

<form method="post">
    <span>
        Date From: @Html.TextBox("StartDate", Model.SD.ToDisplayString(), new { style = "background-color: #FFF; font-size: 80%;" })
        -
        Date To: @Html.TextBox("EndDate", Model.ED.ToDisplayString(), new { style = "background-color: #FFF; font-size: 80%;" })
        <input type="submit" value="Go" />
    </span>
</form>